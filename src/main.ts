try { ... } catch (e) { return; } } function main() { console.log('Starting LLM service'); const model = 'deepseek-r1'; const API_KEY = process.env.NEXT_PUBLIC_API_001; try { const request = new TextRequest({ model, messages: [ { role: 'user', content: 'Hello!' }, { role: 'assistant', content: 'Good day!' } ] }); const response = await request.sendWithToken('llama2'); // Changed to use deepseek-r1 instead of ollama. console.log(response); } catch (error) { console.error('Error:', error); } finally { // ... } } return null;